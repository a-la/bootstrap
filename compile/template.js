const { _bootstrap } = require('./bootstrap')

/**
 * @methodType {_bootstrap.bootstrap}
 */
function bootstrap(config) {
  return _bootstrap(config)
}

module.exports = bootstrap

/* typal types/index.xml namespace */
